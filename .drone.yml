pipeline:
  build:
    image: frekele/ant
    secrets: [ testkey ]
    commands:
      #- ant -version
      #- java -version
      - pwd
      - echo $${TESTKEY}
      - echo ${TESTKEY}
      - ant compile jar run
